lib-cov
*.seed
*.log
*.csv
*.dat
*.out
*.pid
*.gz
*.swp
*.db

pids
logs
results
tmp
dist
.cache
.parcelCache

# Build
public/css/main.css

# Coverage reports
coverage

# API keys and secrets
.env

# Dependency directory
node_modules
bower_components

# Editors
.idea
*.iml
.vscode

# OS metadata
.DS_Store
Thumbs.db

# Ignore built ts files
dist
build
.build
.serverless

# ignore yarn.lock
yarn.lock

# ignore local files
*.LOCAL.*
*.local.*
.tago-lock.dev.lock
.tagoio
exportBackup

# Midscene run output (cache, logs, reports) — all generated, kept local
midscene_run/

# Playwright saved auth sessions (personal tokens — never commit), but keep the
# committed template that documents the session file's shape.
e2e/.auth/*
!e2e/.auth/session.example.json

# Playwright test output (traces, screenshots, last-run) — regenerated each run
test-results/
playwright-report/
